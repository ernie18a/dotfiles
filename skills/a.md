# os: test remove?
# dual lg2: 
- test remove? schema 用減法 來處理 ? 
- schema 的核心目的只有2個一個就是讓 笨蛋人工智慧在寫代碼 執行後有問題的時候聰明人工智慧帶領, 可能也是寫 執行手冊然後叫 笨蛋人工智慧執行 ? 第2個核心目的就是 Check Point是為了預防 當程序被外部打斷的時候總被外部終止的時候 那麼單靠這些非常單薄的 記錄檔可以 利用最少的token來恢復 有可能是類似記錄了 : 有什麼問題 , 改什麼檔案 . 這2個 資訊 , 所以說假設這個時候正要改或者是正在改的時候程序被打斷了 然後他跳出了 然後之後使用者重新執行無腦執行這個程式就是 原本的執行方式然後這個人能人工智慧就去看說上一次在處理的事有什麼問題該什麼檔案然後可能去看這個檔案有沒有 已經解決那個問題了 . 我重新描述 所以這個 schema 不應該 太複雜不應該記錄的太頻繁 . 因為它的核心需求太簡單了 , 除非是編輯或是規範 這個就要拿出來討論了 .
- 便宜笨蛋人工智慧開發完 測試完 然後 再把結果通知聰明人工智慧 或者是 不通知 , 聰明ai會自己用某一種通方式來 得到上一輪 修改的結果 ? 感覺後者比較有效率? 這個是在遇到問題的情況下如果不遇到問題的情況下那麼笨蛋人工智慧就是 按照文件開發然後執行
- 笨蛋人工智慧寫的測試腳本都是邏輯100分的 好像隨便寫隨便過 有沒有關係甚至有關係 然後通過但到最後還是會出錯所以不如什麼測試腳本都不要寫反正都是浪費 反正到最後會即使會過但是真正的城市 還是不會過 所以不如直接笨蛋的公司會執行 他剛才開發或修改的程式如果已經到達了測試的階段 就可以這樣做啊如果還沒有到達測試的階段或者是說可能需要寫一些空殼或者是 mocking 才能夠 得到一定程度的結果的話那就先不用測試 ? 
# subt: 改文件 甚至整合進 ULI. new subt 概念 包含 減法 & 第一原則 
# misc: 在某個地方 user lavel instruction 或者是 deve 要提到 ~/.G/dotfiles/home/bash_profile 裡面大量的環境變數 export?
